# Film posters and stills are resolved against TMDB at build time.
# Get a v3 key at themoviedb.org -> Settings -> API, and copy this file to .env.
# Without it the site still builds; the cabinet just shows plain coloured cases.
TMDB_API_KEY=
